# Default config for EpiSensor MQTT broker
# Inter-snap communication only — not exposed to the network

# Do not store messages to disk
persistence false

# Run as root user (required in snap confinement)
user root

# Allow anonymous connections (authentication is handled at the Edge layer)
allow_anonymous true

# Listen on localhost only — prevents external network access
listener 1883 127.0.0.1
